<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zpark.book.dao.UserDao">
    <resultMap id="BaseResultMap" type="com.zpark.book.entity.User">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="user_password" jdbcType="VARCHAR" property="userPassword"/>
        <result column="nickname" jdbcType="VARCHAR" property="nickname"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="sign_in_time" jdbcType="TIMESTAMP" property="signInTime"/>
        <result column="user_status" jdbcType="TINYINT" property="userStatus"/>
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <result column="birth" property="birth" jdbcType="DATE"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, phone, email, user_password, nickname, avatar, sign_in_time, user_status, sex, birth
    </sql>

    <select id="userLogin" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
        where (phone = #{accountNumber} or email = #{accountNumber})
        and user_password = #{userPassword}
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="countNormalUser" resultType="int">
        select count(*) from sh_user
        where user_status is null or user_status = 0
    </select>

    <select id="countBanUser" resultType="int">
        select count(*) from sh_user
        where user_status = 1
    </select>

    <select id="getUserList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
    </select>

    <select id="getUserByNumber" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
        where phone like concat('%', #{searchValue}, '%')
        and user_status = #{mode}
    </select>

    <!-- 修复：使用 resultMap 而非 resultType -->
    <select id="selectByEmail" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM sh_user WHERE email = #{email}
    </select>

    <select id="selectByPhone" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM sh_user WHERE phone = #{phone}
    </select>

    <select id="countByPhone" resultType="int">
        SELECT COUNT(1) FROM sh_user
        WHERE phone = #{phone}
    </select>

    <!-- 修复：使用 resultMap 而非 resultType -->
    <select id="selectByPhoneAndPassword" resultMap="BaseResultMap">
        SELECT * FROM sh_user
        WHERE phone = #{phone}
          AND user_password = #{password}
    </select>

    <!-- 修复：使用 resultMap 而非 resultType -->
    <select id="selectByEmailAndPassword" resultMap="BaseResultMap">
        SELECT * FROM sh_user
        WHERE email = #{email}
          AND user_password = #{password}
    </select>

    <select id="getNormalUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
        where user_status is null or user_status = 0
        order by id desc limit #{begin}, #{nums}
    </select>

    <select id="getBanUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sh_user
        where user_status = 1
        order by id desc limit #{begin}, #{nums}
    </select>

    <select id="findUserByList" resultMap="BaseResultMap">
        select id, nickname, avatar, sign_in_time
        from sh_user
        where id in
        <foreach collection="idList" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from sh_user
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.zpark.book.entity.User" useGeneratedKeys="true">
        insert into sh_user (phone, email, user_password, nickname, avatar, sign_in_time, sex, birth)
        values (#{phone,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR}, #{userPassword,jdbcType=VARCHAR},
                #{nickname,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR}, #{signInTime,jdbcType=TIMESTAMP}, #{sex,jdbcType=VARCHAR}, #{birth,jdbcType=DATE})
    </insert>

    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.zpark.book.entity.User" useGeneratedKeys="true">
        insert into sh_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="phone != null">phone,</if>
            <if test="email != null">email,</if>
            <if test="userPassword != null">user_password,</if>
            <if test="nickname != null">nickname,</if>
            <if test="avatar != null">avatar,</if>
            <if test="signInTime != null">sign_in_time,</if>
            <if test="sex != null">
                sex,
            </if>
            <if test="birth != null">
                birth,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="phone != null">#{phone},</if>
            <if test="email != null">#{email},</if>
            <if test="userPassword != null">#{userPassword},</if>
            <if test="nickname != null">#{nickname},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="signInTime != null">#{signInTime},</if>
            <if test="sex != null">#{sex},</if>
            <if test="birth != null">#{birth},</if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.zpark.book.entity.User">
        update sh_user
        <set>
            <if test="phone != null">phone = #{phone,jdbcType=VARCHAR},</if>
            <if test="email != null">email = #{email,jdbcType=VARCHAR},</if>
            <if test="userPassword != null">user_password = #{userPassword,jdbcType=VARCHAR},</if>
            <if test="nickname != null">nickname = #{nickname,jdbcType=VARCHAR},</if>
            <if test="avatar != null">avatar = #{avatar,jdbcType=VARCHAR},</if>
            <if test="signInTime != null">sign_in_time = #{signInTime,jdbcType=TIMESTAMP},</if>
            <if test="userStatus != null">user_status = #{userStatus},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="birth != null">birth = #{birth},</if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.zpark.book.entity.User">
        update sh_user
        set phone = #{phone,jdbcType=VARCHAR},
            email = #{email,jdbcType=VARCHAR},
            user_password = #{userPassword,jdbcType=VARCHAR},
            nickname = #{nickname,jdbcType=VARCHAR},
            avatar = #{avatar,jdbcType=VARCHAR},
            sign_in_time = #{signInTime,jdbcType=TIMESTAMP},
            sex = #{sex, jdbcType=VARCHAR},
            birth = #{birth,jdbcType=DATE}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updatePassword">
        update sh_user
        set user_password = #{newPassword,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
          and user_password = #{oldPassword,jdbcType=VARCHAR}
    </update>

    <select id="selectByUserName" resultType="java.lang.Long">
        select id from sh_user where nickname = #{nickname,jdbcType=VARCHAR}
    </select>

    <!-- 更新用户信息（包括状态） -->
    <update id="updateUserInfo" parameterType="com.zpark.book.entity.User">
        UPDATE sh_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="sex != null and sex != ''">sex = #{sex},</if>
            <if test="birth != null">birth = #{birth},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="avatar != null and avatar != ''">avatar = #{avatar},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="userStatus != null">user_status = #{userStatus}</if> <!-- 关键修改：映射到user_status字段 -->
        </set>
        WHERE id = #{id}
    </update>

</mapper>